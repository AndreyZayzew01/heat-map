{
  "name": "heat-map-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev:v1": "npm --prefix apps/ver_1 run dev",
    "dev:v2": "npm --prefix apps/ver_2 run dev",
    "build:v1": "npm --prefix apps/ver_1 run build",
    "build:v2": "npm --prefix apps/ver_2 run build",
    "install:all": "npm --prefix apps/ver_1 install && npm --prefix apps/ver_2 install"
  }
}
